# Cross-Platform | Script 02: Descriptive Comparison
# Author: Sawood Anwar
library(tidyverse); library(janitor)

# combined %>%
#   group_by(platform) %>%
#   summarise(
#     n_posts       = n(),
#     mean_likes    = mean(likes, na.rm = TRUE),
#     mean_comments = mean(comments, na.rm = TRUE),
#     mean_engagement = mean(engagement, na.rm = TRUE)
#   )
cat('Descriptive comparison script ready.\n')
